// import { Given, Then } from 'cypress-cucumber-preprocessor/steps';

// const urlMap = {
//   'checkout page': '/checkout/checkout',
// };

// Given(/^I navigate to the (.*)$/, target => {
//   cy.log('Given is running');
// //   cy.visit(urlMap[target]);
//   cy.visit('/checkout/checkout');
// });

// Then(/I expect a header saying "(.*)"/, headerText => {
//   cy.get('h2').should('have.text', headerText);
// });
